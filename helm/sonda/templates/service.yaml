apiVersion: v1
kind: Service
metadata:
  name: {{ include "sonda.fullname" . }}
  labels:
    {{- include "sonda.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sonda.selectorLabels" . | nindent 4 }}
