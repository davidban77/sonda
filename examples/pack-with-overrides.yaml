# Example: pack with per-metric generator overrides.
#
# The overrides map lets you replace the default generator for specific
# metrics without modifying the pack definition. Here ifOperStatus uses
# a flap alias to simulate interface up/down toggling.
#
# Run with:
#   sonda --catalog sonda-core/tests/fixtures/packs run examples/pack-with-overrides.yaml

version: 2
kind: runnable

defaults:
  rate: 1
  duration: 30s
  encoder:
    type: prometheus_text
  sink:
    type: stdout

scenarios:
  - signal_type: metrics
    pack: telegraf_snmp_interface
    labels:
      device: rtr-edge-01
      ifName: GigabitEthernet0/0/0
      ifAlias: "Uplink to Core"
      ifIndex: "1"
    overrides:
      ifOperStatus:
        generator:
          type: flap
