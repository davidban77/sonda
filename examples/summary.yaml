name: rpc_duration_seconds
rate: 1
duration: 10s

distribution:
  type: normal
  mean: 0.1
  stddev: 0.02

observations_per_tick: 100
seed: 42

labels:
  service: auth
  method: GetUser

encoder:
  type: prometheus_text

sink:
  type: stdout
